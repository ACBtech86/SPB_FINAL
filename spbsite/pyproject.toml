[project]
name = "spbsite"
version = "1.0.0"
description = "SPB (Sistema de Pagamentos Brasileiro) monitoring and message-routing interface"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
